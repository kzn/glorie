package name.kazennikov.glorie.func;

import name.kazennikov.features.Function;

/**
 * Marker interface indicating that a predicate expects an SymbolSpan object as first parameter.
 * Used by the function rewriting.
 */
public interface SymbolSpanInjectable extends Function {

}
